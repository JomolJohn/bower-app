import { NOTIFICATION_TEXT } from '../../constants/constants';
import './Notification.css';

const Notification = () => {
    return (
        <div className='notification'>{NOTIFICATION_TEXT}</div>        
      );
};
  
export default Notification;
